class Tale {
  int taleID;
  String taleName, taleProfileUrl, voicingName, taleUrl;
  double taleLength;

  Tale(this.taleID, this.taleName, this.taleProfileUrl, this.voicingName,
      this.taleUrl, this.taleLength);
}
